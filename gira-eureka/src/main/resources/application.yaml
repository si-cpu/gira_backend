server:
  port: 8761 # Eureka 서버의 포트 번호

eureka:
  client:
    register-with-eureka: false # 자기 자신을 Eureka에 등록하지 않음
    fetch-registry: false       # 다른 서비스의 등록 정보를 가져오지 않음
  server:
    enable-self-preservation: false # 자기 보호 모드 비활성화, 잘못된 등록 정보 제거 가능
  instance:
    hostname: gira-eureka
